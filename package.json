{
  "name": "chrome-extension-timer",
  "version": "1.0.0",
  "private": true,
  "description": "Overlay Timer Chrome extension",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'",
    "test:coverage": "node --test --experimental-test-coverage 'tests/**/*.test.js'"
  }
}
